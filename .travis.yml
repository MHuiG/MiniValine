language: node_js
node_js:
  - 8
os: linux

install:
  - npm install -g eslint
  - npm install

script:
  - npm run build
  - npm test

after_script:
  - npm run check
  - npm run cover
  - bash <(curl -s https://codecov.io/bash)


notifications:
  email: false
