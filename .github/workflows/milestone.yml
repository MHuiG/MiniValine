name: Pull Request Milestone

on:
  pull_request:
    types:
      - opened

jobs:
  milestone:
    name: Pull Request Milestone
    runs-on: ubuntu-latest
    steps:
      - uses: MHuiG/actions-milestone@master
        with:
          repo-token: "${{ secrets.TOKEN }}"
          configuration-path: .github/milestone.yml